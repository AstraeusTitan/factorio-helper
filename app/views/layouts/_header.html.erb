<!-- Header -->
<div class="header">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a href="<%= logged_in? ? '#' : welcome_path %>" class="navbar-brand">Logo</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-collapse">
      <ul class="navbar-nav ml-auto">
        <% if logged_in? %>
          <li class="nav-item <%= set_active_nav_item %>">
            <!-- Update with browse_path when recipes are added -->
            <%= link_to 'Browse', '#', class: 'nav-link' %>
          </li>
          <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" id="navbar-dropdown-link" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= current_user.email.split('@')[ 0 ] %>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbar-dropdown-link">
              <%= link_to 'Account', account_path, class: 'dropdown-item' %>
              <%= link_to 'Log out', logout_path, method: 'delete', class: 'dropdown-item' %>
            </div>
          </li>
        <% else %>
          <li class="nav-item <%= set_active_nav_item welcome_path %>">
            <%= link_to 'Welcome', welcome_path, class: 'nav-link' %>
          </li>
          <li class="nav-item <%= set_active_nav_item signup_path %>">
            <%= link_to 'Sign Up', signup_path, class: 'nav-link' %>
          </li>
          <li class="nav-item <%= set_active_nav_item login_path %>">
            <%= link_to 'Log In', login_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>
</div>
